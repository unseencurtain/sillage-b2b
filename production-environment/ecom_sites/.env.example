# Deprecated — use ../.env.example (single env for the whole stack).
# MariaDB keys still honored if you keep a local ecom_sites/.env for legacy scripts:
MYSQL_ROOT_PWD=change-me
MYSQL_DB=earth
MYSQL_USER=lime
MYSQL_PWD=change-me
