# Copy to secrets.overlay.env (gitignored) or let the dashboard Secrets page create it.
# Compose bind-mounts this file into wholesale-core / wholesale-cron at /app/data/secrets.overlay.env.
#
# WHOLESALE_PERFUMES_USER=
# WHOLESALE_PERFUMES_TOKEN=
