/**
 * Wholesale-perfumes fixtures are the XML samples under tests/fixtures/.
 * Live catalogue dumps belong in .feedscratch (gitignored).
 */
console.log("wholesale-perfumes uses tests/fixtures/wholesale_perfumes_*.xml — no SOAP dump step.");
